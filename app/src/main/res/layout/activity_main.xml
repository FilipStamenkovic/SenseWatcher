<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="nos.elfak.rs.sensewatcher.MainActivity">

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="See Results"
        android:id="@+id/results"
        android:onClick="seeResults"
        />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Listen for anomalies"
        android:id="@+id/anomalies"
        android:onClick="startListening"
        android:layout_below="@+id/results"
        android:layout_alignParentStart="true"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="See Anomalies"
        android:id="@+id/button2"
        android:onClick="seeAnomalies"
        android:layout_below="@+id/anomalies"
        android:layout_alignParentStart="true"/>
</RelativeLayout>
